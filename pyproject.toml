[project]
name = "toon-based-llm-calls"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "toon-format",
]

[tool.uv.sources]
toon-format = { git = "https://github.com/toon-format/toon-python.git" }
