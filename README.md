# toon-based-llm-calls
If your application relies heavily on passing structured data back and forth to an LLM, the cumulative token savings from using TOON over a large number of calls can certainly translate into significant cost reduction.
